# /etc/NetworkManager/conf.d/unmanaged.conf
# Tell NetworkManager not to touch the WireGuard interface.
# enp0s31f6 is managed dynamically by mode.sh (server=static IP, laptop=NM-managed).

[keyfile]
unmanaged-devices=interface-name:wg0
