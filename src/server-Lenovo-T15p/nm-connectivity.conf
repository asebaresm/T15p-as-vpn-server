# /etc/NetworkManager/conf.d/connectivity.conf
# Probe for real internet connectivity so NM can detect a dead/captive building
# WiFi and fail over to the LTE backup automatically.

[connectivity]
uri=http://networkcheck.kde.org/
interval=60
