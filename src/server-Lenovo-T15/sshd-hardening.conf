# /etc/ssh/sshd_config.d/10-t15.conf — managed by t15
#
# Disable all password-based authentication. Only SSH keys are accepted.
# Prerequisite: a working key-based login (verify before deploying).

PasswordAuthentication no
KbdInteractiveAuthentication no
ChallengeResponseAuthentication no
