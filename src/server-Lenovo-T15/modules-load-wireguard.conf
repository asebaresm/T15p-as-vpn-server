# /etc/modules-load.d/wireguard.conf — managed by t15
#
# Force the wireguard kernel module to load early in boot via
# systemd-modules-load.service, so wg-quick@wg0.service never has to race
# against on-demand modprobe (which can fail silently after dirty reboots).

wireguard
